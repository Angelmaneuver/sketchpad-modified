<?php
/**
 * User custom loading stylesheets settings.
 *
 * @package    Sketchpad
 * @subpackage user
 * @since      3.0.0
 */

// @codingStandardsIgnoreStart
return [
	/**
	 * Setting example
	 * [ 'handle'     => Name of the stylesheet. Should be unique.,
	 *   'filename'   => File name of the stylesheet,
	 *   'media'      => The media for which this stylesheet has been defined.
	 *                   Accepts media types like 'all', 'print' and 'screen',
	 *                   or media queries like '(orientation: portrait)' and '(max-width: 640px)'.,
	 *   'target_url' => Specify the url at which you want the stylesheets to be loaded.
	 *                   If you want to load it on all pages, 'all'.
	 *  ]
	 */
];
// @codingStandardsIgnoreEnd
